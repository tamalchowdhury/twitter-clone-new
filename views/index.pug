extends layout 
include mixins/_tweetForm
include mixins/_tweetCard
block content
	.single_wrap
		if user 
			+tweetForm()
		//- - const items = [1,2,3,4,5,6,7,8,9]
		each tweet in tweets
			+tweetCard(tweet)
